.row.max-width
    .card
        if enableDesc
            .card-image
                img(src='/images/planets/#{planet.image}')
                span.card-title #{planet.name}
        else
            a(href='/planet/#{index}').card-image
                img(src='/images/planets/#{planet.image}')
                span.card-title #{planet.name}
        .float-text
            span(data-badge-caption="").badge.new #{planet.class}

        .card-content
            if enableDesc
                each desc in planet.description
                    p #{desc}
        .card-action.row
            p.col.m6 Diametro: #{planet.diameter}km
            p.col.m6 Gravità: #{planet.gravity}m/s
                sup 2